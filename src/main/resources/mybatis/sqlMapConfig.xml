<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--全局setting配置，根据实际需要配置-->

    <!--定义别名-->
    <typeAliases>
        <!--批量扫描别名-->
        <package name="cn.com.yusys.po"/>
    </typeAliases>
    <typeHandlers>
        <!--<typeHandler handler="cn.xyz.typeHandler.MyStringTypeHandler" jdbcType="VARCHAR" javaType="string"/>-->
        <!--<typeHandler handler="org.apache.ibatis.type.EnumOrdinalTypeHandler" javaType="cn.xyz.enums.Sex"/>-->
        <!--<typeHandler handler="org.apache.ibatis.type.EnumTypeHandler" javaType="cn.xyz.enums.Sex"/>-->
    </typeHandlers>

    <!--无需配置了
        使用spring和mybatis的整合包进行mapper扫描，
        但是要遵循一些规则：mapper.xml和mapper.java文件同名并在一个目录

    -->
    <!--<mappers>-->
        <!--
            批量加载mapper
            指定mapper接口的包名，mybatis自动扫描包下边所有mapper接口进行加载
            遵循一些规范：需要将mapper接口类名和mapper.xml映射文件名称保持一致，且在同一个目录中
            上边规范的前提是：使用的是mapper代理方法
        -->
        <!--<package name="cn.sqlmappper"/>-->
    <!--</mappers>-->
</configuration>
































