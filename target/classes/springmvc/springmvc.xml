<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!--通过组件扫描加载Controller
        可以扫描Controller、Service
        此处扫描Controller
    -->
    <context:component-scan base-package="cn.com.yusys.controller"/>

    <!--使用mvc:annotation-driven可以代替1.3和2.3两个注解映射器和注解适配器
        mvc:annotation-driven还默认加载了很多参数绑定，比如JSON转换解析器默认加载了
    -->
    <mvc:annotation-driven conversion-service="conversionService"/>

    <!--视图解析器
        解析jsp视图，默认使用jstl标签，classpath需要含有jstl包
    -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--自定义参数绑定-->
    <!--<bean id="conversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean">-->
        <!--&lt;!&ndash;配置转换器&ndash;&gt;-->
        <!--<property name="converters">-->
            <!--<set>-->
                <!--&lt;!&ndash;日期类型转换&ndash;&gt;-->
                <!--<bean class="cn.xyz.ssm.controller.converter.CustomDateConverter"/>-->
            <!--</set>-->
        <!--</property>-->
    <!--</bean>-->
</beans>